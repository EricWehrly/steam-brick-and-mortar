export const CEILING_DIFFUSE_OPTIONS = {
    width: 512,
    height: 512,
    color: '#E8E6D0',
    bumpDensity: 14,
    bumpHeight: 1.4,
    detailScale: 5,
} as const

export const CEILING_NORMAL_OPTIONS = {
    width: 512,
    height: 512,
    bumpDensity: 14,
    detailScale: 5,
    strength: 20,
} as const
